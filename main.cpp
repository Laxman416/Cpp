
#include<iostream>
#include<string>
#include "Lepton.h" 
#include "ChargedLepton.h"
#include "CalorimeterLayers.h"
#include "Electron.h"
#include "Muon.h"
#include "Tau.h"
#include "ElectronNeutrino.h"
#include "MuonNeutrino.h"
#include "TauNeutrino.h"

// Main program
int main()
{
  // Electron electron(true, 5, 0, 0, 0);
  // Muon muon(true, 0,0,0,0, false);
  // muon.print_data();
  // // electron.print_data();
  // // electron.get_four_momentum_ptr()->set_E(120);
  // electron.get_calorimeter_energies_ptr()->set_calorimeter_energy(10,20,0,0);
  // // electron.get_calorimeter_energies_ptr()->print_calorimeter_info();
  // electron.print_data();
  // Tau tau(true,5,1,2,3,true);
  // tau.print_data();
  // tau.set_is_antiparticle(true);
  // tau.set_is_antiparticle(false);
  // tau.print_data();
  // tau.get_decayed_particle_ptr()->print_data();
  // tau.get_decayed_particle_ptr()->print_data();
  // ElectronNeutrino electron_neutrino(true,5,1,1,1,true);
  return 0;
}